# firebase_notify.py - Placeholder mode
import os
def send_push_notification(title, body, topic='all_users', image_url=None):
    print(f"📱 NOTIFICATION: {title} - {body}")
    return {'success': True}
print("Firebase module loaded (placeholder mode)")
